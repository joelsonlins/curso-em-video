algoritmo "conversor"
var
   angulo, s: Real
inicio
      Escreval("Informe um angulo: ")
      Leia(angulo)
      s <- Sen(graupRad(angulo))
      Escreva("O senho de ", angulo, " é igual a ", s)

fimalgoritmo